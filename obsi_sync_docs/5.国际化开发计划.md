# 国际化开发计划

> 创建日期：2026-07-23
> 最近更新：2026-07-23

## 文档职责

本文只维护国际化的实施阶段、开发进度、验证记录和下一步。

稳定的设计目标、结构、功能契约和使用方式见 `4.国际化设计.md`。设计变化先修改设计文档，再同步调整本文计划；仅开发进度变化时只修改本文。

## 总体状态

- 当前阶段：第一阶段，泛用模块。
- 总体状态：已暂停，备份功能优先。
- 页面接入状态：尚未开始。
- 预计总工作量：16 至 25 小时。

## 暂停记录

- 暂停日期：2026-07-23。
- 暂停原因：项目先开发备份功能，国际化后续恢复。
- 暂停位置：第一阶段泛用模块尚未完成。
- 恢复原则：继续按 `4.国际化设计.md` 实施，不重新设计已确认的契约。

## 代码收纳状态

- 原型代码集中在 `frontend/src/lib/i18n/`。
- 当前包含 `create_i18n.svelte.ts`、`messages.ts`、`index.svelte.ts`。
- 业务页面和组件没有引用国际化实例。
- 原型不会影响当前备份功能开发。
- 暂停期间不继续拆分 `core/`、迁移 `infa-s5` 或接入页面。

## 已完成原型

- [x] 实现项目内语言状态和系统语言检测原型。
- [x] 实现项目内用户语言持久化原型。
- [x] 将两个原型重构进泛用 `createI18n()` 工厂。
- [x] 通过 `index.svelte.ts` 创建当前项目唯一的国际化实例。

现有原型尚未达到第一阶段验收标准。

## 第一阶段：泛用模块

状态：已暂停。

预计工作量：3 至 5 小时。

- [ ] 建立 `frontend/src/lib/i18n/core/`。
- [ ] 将工厂、类型和插值分别放入 `create_i18n.svelte.ts`、`types.ts`、`interpolate.ts`。
- [ ] 使用泛型 `CreateI18nOptions<T, TDefaultLocale>` 保留项目文字表的字面量类型。
- [ ] 从 `T` 推导 Locale，从 `T[TDefaultLocale]` 推导 MessageKey，禁止退化为 `string`。
- [ ] 拆分类型基准语言 `defaultLocale` 和运行时回退语言 `fallbackLocale`。
- [ ] 约束 `defaultLocale`、`fallbackLocale` 和 `htmlLang` 的语言键为 `keyof T`。
- [ ] 实现“当前语言 → fallbackLocale → 原始 key”的文案查找顺序。
- [ ] 实现基于 `Record<string, string | number>` 的轻量参数插值。
- [ ] 将系统语言匹配改为基于项目支持语言。
- [ ] 将存储键、fallback 和 HTML 映射改为配置项。
- [ ] 在实例初始化和 `setLocale()` 时自动同步 `<html lang>`。
- [ ] 将现有语言状态和持久化原型接入最终工厂。
- [ ] 更新本项目 `index.svelte.ts` 实例配置。

完成标准：

- 泛用能力全部位于 `i18n/core/`，不包含项目文案和固定语言。
- `i18n.t()` 只接受基准语言中存在的翻译 key。
- 无效语言配置在 TypeScript 检查阶段报错。
- 缺失翻译、参数插值和 HTML lang 同步符合设计文档。

## 第二阶段：本地基础接入

状态：未开始。

预计工作量：2 至 3 小时。

- [ ] 验证首次加载时 `<html lang>` 与检测到的语言一致。
- [ ] 在应用 Header 右上角增加语言切换按钮。
- [ ] 验证系统语言、用户设置和 fallback 优先级。
- [ ] 确认切换语言不会重置业务状态。

完成标准：

- 软件可以按系统语言启动。
- 用户可以切换语言并保留选择。

## 第三阶段：本地 UI 文案迁移

状态：未开始。

预计工作量：4 至 6 小时。

- [ ] 替换全部 Svelte 页面和组件中的静态 UI 文案。
- [ ] 国际化文件选择对话框。
- [ ] 国际化 `title` 和 `aria-label`。
- [ ] 处理动态数量和英文单复数。
- [ ] 搜索并清理遗漏的硬编码 UI 文案。

完成标准：

- 中英文界面主体均可完整使用。

## 第四阶段：动态文案与错误

状态：未开始。

预计工作量：5 至 8 小时。

- [ ] 增加用于前端展示的 `DescriptionKey`。
- [ ] 保留带 `json:"-"` 的 `Description` 作为 Go 内部 fallback。
- [ ] 补充配置项及插件说明的本地翻译。
- [ ] 更新 Wails bindings。
- [ ] 为面向 UI 的后端业务错误增加稳定错误码。
- [ ] 实现前端错误码翻译和未知错误兜底。

完成标准：

- 后端只向前端返回 `DescriptionKey`，不序列化内部 `Description` fallback。
- 已知业务错误使用当前界面语言。

## 第五阶段：验证并迁移到 infa-s5

状态：未开始。

预计工作量：2 至 3 小时。

- [ ] 使用中文和英文完整走通 M1 至 M5。
- [ ] 检查英文长文案和最小窗口布局。
- [ ] 执行后端核心回归测试及必要的前端构建检查。
- [ ] 将 `frontend/src/lib/i18n/core/` 整体移动到 `infa-s5/src/lib/i18n/core/`。
- [ ] 从 `infa-s5` 导出 `createI18n()`、相关类型和插值能力。
- [ ] 当前项目改为从 `infa-s5` 导入。
- [ ] 删除当前项目中的 `i18n/core/` 副本。
- [ ] 更新项目功能状态文档。

完成标准：

- `infa-s5` 提供稳定的泛用国际化能力。
- 当前项目只保留本地文字表和实例配置。
- 其他项目可以通过相同方式接入。

## 恢复后的下一步

1. 建立 `frontend/src/lib/i18n/core/` 目录结构。
2. 拆分 `types.ts` 和 `interpolate.ts`。
3. 调整 `createI18n()` 的泛型与 default/fallback 语言契约。

## 验证记录

当前尚未执行正式验证。
