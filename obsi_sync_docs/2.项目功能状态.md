# 项目功能状态

> 最近更新：2026-07-25

## 当前目标

Obsidian 配置同步 MVP 和备份功能已实现，当前优先进行备份功能人工验证。

1. 人工验证备份创建、结果页入口和旧备份清理。
2. 根据验证结果修正备份功能。
3. 国际化暂缓，后续恢复。

备份系统设计见 `6.备份系统设计.md`。

## 已实现功能

- M1：扫描 Obsidian 库列表，已验证。
- M2：前端主库/从库选择状态，已验证。
- M3：同步范围选择，已验证。
- M4：同步计划已改为按从库展示有序同步项及新增/覆盖标记，已于 2026-07-23 人工验证通过。
- M5：同步执行结果已改为按同步项有序展示新增成功、覆盖成功或失败；UI 已于 2026-07-21 手动复核通过，后端核心回归测试已于 2026-07-23 通过。

## 工程状态

- 已建立前端样式分层约定。
- 全局颜色、间距、圆角等设计变量统一维护在 `frontend/src/app.css`。
- 业务页面保留 scoped 布局样式，通用控件继续优先使用 `infa-s5`。
- 应用已启用 Wails 单实例约束；重复启动时恢复并聚焦现有窗口。
- 备份创建、Manifest、临时目录清理、保留 10 次及结果页打开入口已实现，尚未人工验证。
- 国际化已暂停在泛用模块第一阶段；原型代码独立保留且未接入业务页面。设计见 `4.国际化设计.md`，暂停状态见 `5.国际化开发计划.md`。

## 后端核心回归测试

- 测试入口：`go_src/inner/svc/api_test.go`
- 测试数据：`test_cases/README.md`
- 执行命令：`go test ./go_src/inner/svc`
- 2026-07-20 已通过后端核心回归测试。
- 2026-07-23 同步计划模型调整后重新执行后端核心回归测试，测试通过。
- 测试使用 `test_cases/vault1` 作为主库，`vault2`、`vault3` 作为从库的临时副本，不修改版本控制中的基准数据。
- 固定覆盖：文件新增、文件覆盖、目录递归复制、目录部分覆盖、目标额外文件保留、多目标执行，以及 `themes` 目录与目标文件的类型冲突。
- `vault2` 预期无失败项；`vault3` 预期 `themes` 产生一个失败项，其余选中配置成功执行。

## Windows 视口缩放状态

- 已定位 WebView2 `PutRasterizationScale(double)` 的 Win64 ABI 参数错误：必须传递 `math.Float64bits(scale)`，不能传递 `&scale`；修复已进入 Wails 主分支，但尚未包含在项目当前使用的版本中。
- 项目当前固定使用上游 Wails v3 `v3.0.0-alpha2.117`，`go.mod` 不再使用本地 Wails/WebView2 replacement。
- Wails v3 保留上游“仅 DPI 实际变化时同步”的逻辑，不在 `navigationCompleted()` 中无条件重同步或调用 `Resize()`。
- 2026-07-17 在 Windows 150% 缩放下单实例验证通过：冷启动、连续刷新、Vite HMR、最小化/恢复均保持 `dpr=1.5`、`innerWidth=1166`；跨不同 DPI 显示器拖动尚未验证。
- 详细根因、复现指标和后续多显示器定位方式见 `3.Wails_WebView2_视口缩放问题.md`。

## 当前最小设计

| 版本 | 阶段 | 需求 | 后端接口 | 核心规则 | 状态 |
| --- | --- | --- | --- | --- | --- |
| M1 | 解析库列表 | 前端用户指定一个路径，解析 Obsidian 库并显示列表 | `ScanVaults(root string) ([]VaultInfo, error)` | `root/.obsidian/` 存在时 root 是 vault；否则最多检查到 `root/*/*/.obsidian/`；扫描子目录时跳过隐藏目录、`node_modules`、`.git` | 已验证 |
| M2 | 设置主从 | 前端用户点击一个库作为主库，多选几个库作为从库 | 无 | M2 只做前端状态管理，不新增后端接口，不做持久化 | 已验证 |
| M3 | 设置同步文件 | 展示主库里的配置文件，用户选择需要同步的文件 | `ListConfigItems(vaultPath string) ([]ConfigItem, error)` | 同步路径使用相对 `.obsidian` 的路径；默认只勾选通用配置白名单，其余配置允许用户手动勾选 | 已验证 |
| M4 | 展示同步计划 | 执行前校验并展示新增、覆盖等计划 | `BuildSyncPlan(req SyncRequest) (SyncPlan, error)` | 只计算，不写文件；校验主库和从库路径有效；主库不能在从库列表里；按目标库路径是否存在区分新增和覆盖 | 已验证 |
| M5 | 执行同步 | 用户确认计划后复制主库文件到目标库并返回结果 | `ExecuteSyncPlan(plan SyncPlan) (SyncResult, error)` | 执行参数使用 M4 生成的计划，避免展示计划和实际执行不一致；拒绝前端抖动造成的并发重复执行 | UI 与后端核心回归均已验证 |

## M1 约定

返回结果给前端展示。

```go
type VaultInfo struct {
	Path string `json:"path"`
	Name string `json:"name"`
}
```

发现某个目录已经是 vault 后，默认不继续深入该 vault 内部扫描。

M1 不做深层递归扫描。用户选择的目录应满足以下形态之一，最多识别 2 层子目录：

```text
root/.obsidian
root/<vault>/.obsidian
root/<group>/<vault>/.obsidian
```

## M2 约定

M2 只做前端选择状态，不增加后端接口。

前端状态：

```text
mainVault
targetVaults
```

用户流程：

```text
扫描 vault
展示 vault 列表
点击一个 vault 作为主库
多选若干 vault 作为从库
进入 M3 设置同步文件
```

库列表持久化不属于 M2，后续作为优化项处理。

当前 UI 骨架：

```text
SyncWizard
扫描库 -> 选择库 -> 同步范围 -> 同步计划 -> 执行结果
```

当前已落地：

```text
扫描库
多次扫描会合并新发现的 vault，并按 path 去重
选择主库
选择多个从库
每次选择主库后，其他 vault 自动设为从库
主库不能同时作为从库
重新扫描不会清空已选择的主库和从库
```

库选择三态规则：

```text
主库：mainVault.path === vault.path
从库：targetVaults 包含 vault.path
未选中：既不是主库，也不在 targetVaults
```

交互规则：

```text
点击“主库”：当前 vault 设为主库，其他 vault 默认设为从库
点击“从库”：在从库和未选中之间切换
主库不能点成从库；如果要排除当前主库，必须先选择别的主库
```

相关前端文件：

```text
frontend/src/pages/sync_wizard.svelte
frontend/src/pages/scan_vaults.svelte
frontend/src/pages/steps/select_vaults_step.svelte
frontend/src/lib/components/vault_list.svelte
frontend/src/lib/components/step_nav.svelte
```

M2 已于 2026-07-15 人工验证通过。

## M3 约定

同步文件使用相对 `.obsidian` 的路径，不使用绝对配置文件路径。

示例：

```text
appearance.json
hotkeys.json
plugins/dataview/
```

默认只勾选以下通用配置：

```text
app.json
core-plugins.json
hotkeys.json
appearance.json
snippets/
themes/
```

`appearance.json` 记录启用的 CSS 片段和主题，应与 `snippets/`、`themes/` 一起同步。

其他配置仍正常展示，但默认不勾选，允许用户手动勾选。

常见的社区插件列表、图谱、书签、属性类型和工作区配置提供用途及跳过原因说明。

默认规则只在用户首次进入同步范围设置时应用一次，后续允许用户编辑。

后端接口：

```go
type ConfigItem struct {
	Path            string `json:"path"`
	Name            string `json:"name"`
	Version         string `json:"version"`
	IsDir           bool   `json:"isDir"`
	Description     string `json:"description"`
	DefaultSelected bool   `json:"defaultSelected"`
	IsPlugin        bool   `json:"isPlugin"`
}

ListConfigItems(vaultPath string) ([]ConfigItem, error)
OpenVaultConfigDir(vaultPath string) error
```

当前扫描范围：

```text
.obsidian 根目录文件
.obsidian 根目录一级目录；其中 plugins/ 不作为整体返回
.obsidian/plugins/<pluginId>/ 插件目录
.obsidian/plugins/ 下的非目录文件不返回
```

前端状态：

```text
configItems
selectedPaths
```

相关前端文件：

```text
frontend/src/pages/steps/select_scope_step.svelte
frontend/src/pages/sync_wizard.svelte
frontend/src/lib/api/vault_service.ts
frontend/bindings/obsi-conf-sync/go_src/inner/svc/models.ts
```

相关后端文件：

```text
go_src/inner/svc/vaultservice.go
go_src/conf/sync_rules.go
```

当前实现说明：

```text
进入第三步后按 mainVault.path 调用 ListConfigItems
首次加载时 selectedPaths 默认只包含通用配置白名单
配置项说明和默认选择规则由后端 conf/sync_rules.go 统一维护
用户可手动勾选/取消勾选配置项
已勾选配置显示在“同步”区域，未勾选配置显示在分隔后的“跳过”区域
三个配置列表均使用 item.path 作为 each key，避免配置项跨分组移动时 checkbox 状态串用
配置项卡片使用横向三列：配置项、说明、未来功能预留区；窄屏改为单列
community-plugins.json 和 plugins/<pluginId>/ 统一显示在独立的“社区插件”区域
社区插件区域显示已选择数量，插件默认不勾选，第一版不做自动联动
插件名称和版本读取自 plugins/<pluginId>/manifest.json，读取失败时使用插件目录名
插件提示格式为“社区插件 · <name> · v<version>”，缺少版本时不显示版本
社区插件区域提示用户根据需要手动选择要同步的插件
顶部按钮顺序：打开配置文件夹 | 默认 | 全选
SyncWizard 顶部全局摘要已移除，M3 不再重复展示主库、从库、已发现和同步项统计
M3 标题下使用“主库”标签和加粗库名突出配置来源，悬停库名可查看完整路径
打开配置文件夹会使用系统文件管理器打开当前主库的 .obsidian 目录
切换主库后会清空 configItems 和 selectedPaths
```

开发启动预设：

```text
开发环境在 M1 显示“开发：快速进入 M3”按钮
点击后先调用 `DevService.ResetTestCases()` 重建固定测试场地 `temp/test_cases_1`，再扫描该目录，将 vault1 设为主库，vault2、vault3 设为从库
默认选择与核心回归测试一致的 app.json、community-plugins.json、snippets/、themes/ 和两个固定插件目录
进入 M3 后仍允许手动调整同步范围
执行同步只修改 `temp/test_cases_1/vault2`、`vault3`；点击完成后调用 `DevService.RemoveDirectory()` 清理固定测试场地
`temp/` 已加入 `.gitignore` 和 Wails 开发监视忽略，测试副本不会进入版本控制，也不会触发开发进程重启
按钮不在生产环境渲染；执行失败时停留在 M1 并显示错误
```

WebView2 远程调试：

```text
默认不开放远程调试端口
需要连接 Chrome DevTools MCP 时，在项目根目录 .env.local 中设置 OBSI_WEBVIEW_DEVTOOLS=1
重新启动 task dev 后开放固定端口 9222；删除配置或改为 0 后关闭
根目录 .env.local 仅供本机 Wails 开发配置使用，已加入 .gitignore
```

验证注意：

```text
M3 的 Go 返回模型已新增 Description、DefaultSelected、IsPlugin、Version 字段
2026-07-12 已执行 task bindings，当前 frontend/bindings 已更新
task dev 已运行时需要重启，前端才能使用最新 ListConfigItems 模型
后续新增或修改 Wails 后端接口后，AI 应主动执行 task bindings，无需再次征求用户同意
M2、M3 已于 2026-07-15 人工验证通过
普通配置跨“同步/跳过”分组后的视觉串选问题已复验通过
```

下一步接手建议：

```text
1. 使用开发预设完整执行一次同步，检查备份目录、Manifest 和结果页打开入口
2. 验证全部为新增项时不创建空备份
3. 验证符号链接会中止备份和同步
4. 验证完整备份超过 10 个后只保留最新 10 个
```

## M4 约定

`BuildSyncPlan` 只计算同步计划，不写文件，不创建备份目录。

计划需要展示：

```text
主库路径
每个目标库将新增哪些配置
每个目标库将覆盖哪些配置
```

当前最小模型：

```go
type SyncRequest struct {
    MainVaultPath    string   `json:"mainVaultPath"`
    TargetVaultPaths []string `json:"targetVaultPaths"`
    SelectedPaths    []string `json:"selectedPaths"`
}

type SyncPlan struct {
    MainVaultPath string           `json:"mainVaultPath"`
    Targets       []TargetSyncPlan `json:"targets"`
}

type SyncPlanAction string

const (
    SyncPlanActionCreate    SyncPlanAction = "create"
    SyncPlanActionOverwrite SyncPlanAction = "overwrite"
)

type SyncPlanItem struct {
    Path   string         `json:"path"`
    Action SyncPlanAction `json:"action"`
}

type TargetSyncPlan struct {
    VaultPath string         `json:"vaultPath"`
    Items     []SyncPlanItem `json:"items"`
}
```

每个目标库的 `Items` 按 M3 选择顺序完整列出同步项。目标路径不存在时标记为 `create`，存在时标记为 `overwrite`，不增加第三种计划状态。

当前实现文件：

```text
go_src/inner/svc/sync_models.go
go_src/inner/svc/sync_plan.go
frontend/src/lib/api/vault_service.ts
frontend/src/pages/steps/sync_plan_step.svelte
```

前端计划页在目标库列表前显示简洁的主库来源卡片；每个目标库按顺序显示完整同步项列表，每项标记“新增”或“覆盖”，不再拆成两个状态分组。

危险操作第一版只包含：镜像模式下删除从库多余配置文件。

MVP 第一版只实现覆盖模式，因此 M4 第一版不处理镜像模式危险操作。

## M5 约定

`ExecuteSyncPlan` 只接收 `SyncPlan`，不重新接收零散参数。

M5 第一版执行覆盖同步，不包含备份功能。

当前结果模型：

```go
type SyncResult struct {
    BackupPath string             `json:"backupPath"`
    Targets    []TargetSyncResult `json:"targets"`
}

type SyncResultStatus string

const (
    SyncResultStatusCreated   SyncResultStatus = "created"
    SyncResultStatusOverwrote SyncResultStatus = "overwrote"
    SyncResultStatusFailed    SyncResultStatus = "failed"
)

type SyncResultItem struct {
    Path   string           `json:"path"`
    Status SyncResultStatus `json:"status"`
    Error  string           `json:"error"`
}

type TargetSyncResult struct {
    VaultPath string           `json:"vaultPath"`
    Items     []SyncResultItem `json:"items"`
}
```

当前实现：

```text
后端：
go_src/inner/svc/sync_models.go
go_src/inner/svc/sync_execute.go
go_src/inner/svc/sync_backup.go
go_src/inner/svc/vaultservice.go

前端：
frontend/src/lib/api/vault_service.ts
frontend/src/pages/sync_wizard.svelte
frontend/src/pages/steps/sync_result_step.svelte
```

执行规则：

```text
文件和目录均支持复制
目录复制采用递归覆盖，不删除目标目录中的额外文件
执行前根据前端计划重新生成当前计划；空计划、重复目标、非法同步路径会被拒绝
当前计划与前端确认的计划不一致时，返回“同步计划已变化，请重新确认”，且不写文件
执行只使用后端重新生成的当前计划，不直接信任前端传入的路径和新增/覆盖状态
同步计划只承诺路径及新增/覆盖状态，不冻结源文件内容；校验后读取失败仍记录为单项失败
执行顺序与 M4 中每个目标库的同步项展示顺序一致
执行中通过原子状态拒绝新的 ExecuteSyncPlan 调用，防止前端抖动造成并发重复执行
单个目标库的每个同步项按执行顺序记录在 SyncResult.Items 中，状态为新增成功、覆盖成功或失败
前端确认同步后进入结果页，展示总体及各目标库结果
结果页按 M4 相同顺序展示每个同步项，并显示新增成功、覆盖成功或失败；失败项保留错误文字
结果页不提供“上一步”，仅以“完成”结束流程；点击后保留已扫描的库列表，清空本轮同步状态并返回扫描库步骤
```

## 同步模式

### 覆盖模式

主库选中的配置文件覆盖到从库。

MVP 第一版只实现覆盖模式。

### 镜像模式

从库多余的配置文件也删除，使其和主库一致。

镜像模式暂不进入 MVP 第一版，后续实现前需要先完善危险操作确认。

## 后续优化

### 同步范围持久化

```text
保存用户上次选择
```

### 备份功能

M5 第一版同步执行已完成，备份功能已按设计实现，尚未人工验证。

执行边界、目录结构、保留策略和结果契约见 `6.备份系统设计.md`。

2026-07-25 已执行 `task bindings`，Wails 前端绑定生成成功。本轮未运行测试或应用。

### 其他优化

```text
错误提示
记录日志
库列表持久化
Wails 开发约束文档
```

## 执行计划示例

主库：

```text
D:\Obsidian\Main
```

目标库：

```text
D:\Obsidian\Work
D:\Obsidian\Study
```

将复制：

```text
appearance.json
hotkeys.json
plugins/dataview/
```

将跳过：

```text
workspace.json
workspaces.json
```
