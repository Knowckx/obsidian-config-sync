# 项目功能状态

> 最近更新：2026-08-28
> 状态口径：以下“已实现”按当前代码判断；“已验证”仅表示已有历史验证记录。

## 当前目标

人工验证同步前备份功能，并根据验证结果修正实现。

当前代码已包含备份创建、Manifest、目录校验和结果页打开入口，但备份专项仍没有完成完整人工验收；备份相关测试代码暂未提供。

验证重点：

1. 覆盖同步前成功创建完整备份，结果页可以打开备份目录。
2. 全部为新增项时不创建空备份。
3. 备份失败时不执行任何同步写入。
4. 超过 10 个完整备份后只保留最新 10 个。
5. 符号链接相关边界符合预期。

备份契约见 `6.备份系统设计.md`。

## 当前状态

### 已实现（核心流程有历史验证记录）

- 扫描 Obsidian Vault：支持扫描根目录自身及最多两层子目录，跳过隐藏目录、`.git` 和 `node_modules`。
- 主库与多从库选择：主库不会同时作为从库，重新扫描会合并 Vault 并保留已有选择。
- 同步范围选择：列出 `.obsidian` 根配置项和社区插件目录，应用默认选择规则，支持手动调整。
- 同步计划：按从库和用户选择顺序展示新增、覆盖项；生成计划时不写文件。
- 覆盖同步：执行前重新生成并核对计划，拒绝重复目标和并发重复执行；目录采用递归覆盖，不删除从库额外文件。
- 执行结果：按计划顺序展示新增成功、覆盖成功和失败项。

### 已实现，待人工验证

- 同步前备份：仅备份从库中即将被覆盖的文件或目录；全部为新增项时不创建备份。
- 完整备份使用临时目录创建，成功后再重命名为正式目录，并生成 `manifest.json`。
- 备份根目录的完整备份识别和最新 10 个保留清理已实现；当前只有本次实际创建备份时才触发清理，全部为新增项时不会触发旧备份清理。
- 备份阶段会拒绝待覆盖从库目标中的符号链接和特殊文件；任一备份失败会中止整次同步。
- 结果页在存在备份时展示备份路径，并可通过受限后端接口打开完整备份目录。
- 开发环境提供固定测试场地，可快速进入同步范围并在流程结束后清理副本。

### 暂不实施

- 国际化：当前不投入开发或业务接入，项目继续使用现有中文界面；上线后根据实际用户语言需求重新评估，只有出现较多明确需求时才启动。

设计说明和未来评估计划见 `4.国际化设计.md`、`5.国际化开发计划.md`。

## 当前缺口

- 符号链接防护尚未覆盖全部同步路径：当前备份阶段只检查从库覆盖项，全部为新增的目标项、主库源配置项、计划生成和实际复制阶段没有统一的符号链接拒绝策略；实际复制仍使用会跟随链接的文件访问路径。
- 仅支持覆盖模式；镜像模式及危险删除确认尚未实现。
- 不支持自动恢复或一键回滚，备份只能人工恢复。
- Vault 列表、主从选择和同步范围不会持久化。
- 国际化暂不纳入当前版本；业务界面和后端错误继续使用固定中文文本，现有国际化原型仅作未来备查，不作为当前功能承诺。
- 窗口标题和应用描述仍使用 Wails 示例内容。
- Windows 不同 DPI 显示器之间拖动窗口尚未验证，详见 `3.Wails_WebView2_视口缩放问题.md`。

## 关键代码入口

### 后端

- `go_src/inner/svc/vaultservice.go`：Vault 扫描、配置项扫描和目录打开。
- `go_src/inner/svc/sync_plan.go`：同步计划生成与路径校验。
- `go_src/inner/svc/sync_execute.go`：计划复核、同步执行和结果生成。
- `go_src/inner/svc/sync_backup.go`：备份创建、校验、保留清理和目录打开。
- `go_src/conf/sync_rules.go`：配置项说明与默认选择规则。

### 前端

- `frontend/src/pages/sync_wizard.svelte`：五步同步流程及业务状态。
- `frontend/src/pages/steps/`：主从库、同步范围、计划和结果页面。
- `frontend/src/lib/api/vault_service.ts`：Wails 服务适配。
- `frontend/src/lib/i18n/`：尚未接入业务、暂不继续开发的国际化原型。

## 最近已知验证记录

- 2026-07-23：同步核心回归测试通过；覆盖文件新增、覆盖、目录递归、多目标和文件类型冲突等场景。
- 2026-07-23：同步计划与结果页面完成人工验证。
- 2026-07-25：备份接口 bindings 生成成功；备份功能尚未运行人工验证。
- 2026-07-17：Windows 150% 缩放下，冷启动、刷新、热更新和最小化恢复验证正常。

## 最近代码核对

- 2026-08-28：确认 `frontend/src/lib/i18n/` 仍只有原型文件，没有 `core/` 拆分、业务页面引用或 HTML `lang` 同步；按当前决策暂不继续拆分或迁移。
- 2026-08-28：确认备份代码已接入 `ExecuteSyncPlan`、结果页和打开目录接口，但没有备份专项测试；完整符号链接防护仍未接入实际复制路径。

测试入口为 `go_src/inner/svc/api_test.go`，测试数据说明见 `test_cases/README.md`。
